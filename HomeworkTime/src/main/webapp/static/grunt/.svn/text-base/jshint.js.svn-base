module.exports = {
    validate: {
        options: {
            curly: true,
            eqeqeq: true,
            eqnull: true,
            reporter: require('jshint-stylish')
        },

        files: {
            src: ['scripts/**/*.js', '!scripts/lib/**/*.js', '!scripts/templates/**/*.js']
        }
    }
};
